#include "main.h"
#include <stdlib.h>
/**
 * main: Entry point
 * description: function that computes the absolute value of an integer
 * Return: Always 0
 */
int _abs(int i)
{
	_abs(i);
	return i;
}
