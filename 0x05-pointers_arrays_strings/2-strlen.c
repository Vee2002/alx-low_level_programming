#include "main.h"
/**
*_strlen - Length of string
*@s: First parameter
*Return: returns integer value
*/
int _strlen(char *s)
{
char *s[] = "String length";
int len;

len = strlen(*s[]);
return (len);
}
