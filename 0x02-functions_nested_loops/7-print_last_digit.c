#include "main.h"
/**
 * main - Entry point
 * description: function that prints the last digit of a number.
 * Return: Always 0
 */
int print_last_digit(int x)
{
	int y;
	y = x % 10;
	_putchar('y');
	return (y);
}
