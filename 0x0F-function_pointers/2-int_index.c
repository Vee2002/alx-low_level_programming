#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}#include <stdio.h>

/**
 * int_index - Searches for an integer
 * @array: Array of integers
 * @size: Size of integers
 * @cmp: Pointer to the function to compare values
 * Return: returns 1 if no index matches
 * and if size <= 0
 */

int int_index(int *array, int size, int (*cmp)(int))
{
	int i;
	if (array && size && cmp > 0)
		for (i = 0; i < size; i++)
			if ((cmp)(array[i]))
				return (i);
	return (-1);
}
