#include <string.h>
#include "main.h"

/**
 * _strcmp - Compares strings
 * @s1: First parameter
 * @s2: Second parameter
 * Return: returns an integer
 */

int _strcmp(char *s1, char *s2)
{
	int res = strcmp(s1, s2);
	return (res);
}
