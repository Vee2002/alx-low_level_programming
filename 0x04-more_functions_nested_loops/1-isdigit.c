#include "main.h"

/**
*int _isdigit(int c) - Function to check digits
*@c : parameter passed to function
*Return: 1 if c is digit, 0 if otherwise
*/

int _isdigit(int c)
{
if (c >= 0 && c <=9)
{
return (1);
}
else 
{
return (0);
}
}
