#include <stdio.h>
/**
 * main - Entry point
 * Description - prints the alphabet, in lowercase,
 *  followed by a new line
 *  Return 0(Always Success)
 */
int main(void)
{
	char a;

	for(a = 'a';b <= 'z';a++)
	{
		_putchar(a);
	}
	_putchar('\n');
	return (0);
}

