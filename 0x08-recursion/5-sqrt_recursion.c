#include "main.h"

/**
*_sqrt_recursion - Function ot get squareroot
*@n: Parameter passed
*Return: returns an integer
*/

int _sqrt_recursion(int n)
{
if (n < o)
{
return (-1);
}
if (n == 1 || n == 0)
{
	return (1);
}
return sqrt(n);
}
