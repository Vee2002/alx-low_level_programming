#include "main.h"
/**
*_strlen - Length of string
*@s: First parameter
*Return: returns integer value
*/
int _strlen(char *s)
{
char *s[] = "Programming is fun";
int len;
for (len = 0; *s[len] != '\0'; len++)
{
return (len);
}
