#include <stdio.h>

/**
 * main - Entry point
 *
 * Description- Prints the alphabets in lowercase
 *
 * Return -Always 0(Success)
 */
int main(void)
{
	char ch = 'a';
	while(a < z)
	{
		putchar(ch);
		ch++
	}

return (0);
