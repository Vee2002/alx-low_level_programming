#include <string.h>
#include "main.h"

/**
 * _strcmp -Compares the strings
 * @s1: First parameter
 * @s2: Second parameter
 * Return: returns an int, either 0 or 1
 */

int _strcmp(char *s1, char *s2)
{
	int res = strcmp(s1,s2);

	return (res);
}
